// LighthouseTerror configuration — edit this one line to track a different repo.
export const REPO_JSON_URL = "https://raw.githubusercontent.com/MTVirux/SeaOfTerror/main/repo.json";

// Plugins to hide from the dashboard, matched by InternalName (case-sensitive).
// Use this for third-party plugins listed in repo.json that you don't want
// surfaced in the hero tiles, trends, or the plugin table.
export const IGNORED_PLUGIN_INTERNAL_NAMES = ["IINACT"];
